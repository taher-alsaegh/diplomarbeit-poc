{{- if eq (include "authz.renderManagement" .) "true" }}
{{- $clusterId := required "clusterId is required" .Values.clusterId -}}
{{- $clusterName := default .Values.clusterId .Values.clusterName -}}
{{- range $p := .Values.projects }}
---
apiVersion: management.cattle.io/v3
kind: Project
metadata:
  name: {{ $p.name }}
  namespace: {{ $clusterId }}
  labels:
    {{- include "authz.labels" $ | nindent 4 }}
spec:
  clusterName: {{ $clusterName }}
  displayName: {{ $p.displayName | default $p.name | quote }}
{{- end }}
{{- end }}
